class Background {
    constructor() {
        this.paths = [] // This will contain the background paths in an array.
        this.path = this.paths[0] // This will show the starting screen's background in default.
        // above will be used with loadImage()
    }
}